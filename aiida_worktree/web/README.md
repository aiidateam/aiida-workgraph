

## Frontend
Use `rete-kit` to create the frontend, using `react`

```console
npm i -g rete-kit
rete-kit frontend
```


## Backend
Use `FastAPI`

The documentation page is `http://localhost:8000/docs`.
